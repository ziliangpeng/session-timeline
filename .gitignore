.venv/
__pycache__/
*.pyc
*.egg-info/
dist/
build/
.pytest_cache/
trace.json
.DS_Store

# secrets / local env — never commit machine configuration
.env
.env.*

# logs and generated artifacts
*.log

# prototype playground — real session data, never committed
/experimental/

# server cache / generated shards, if a backend ever writes them
/detail/
*.db
